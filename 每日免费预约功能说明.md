# 每日前N名预约用户免费 —— 功能说明

## 一、功能概述

**每天**（按下单当天）**前 100 名（可配置）不同的预约用户**，预约**当天**的日期时享受免费，免费订单直接确认生效，**不走微信支付流程**。名额每天刷新，次日重置。

### 规则明细

- 按「去重用户」（`wechatOpenId`）计数，而非按订单数计数
- **仅当预约日期为今天（当天）时才有免费资格**；预约未来日期一律收费
- 同一用户当天最多享受一次免费，再次预约自动收费
- 免费订单：`isFree=true`、`amount=0`、`status=confirmed`、`paymentStatus=paid`，创建即生效
- 收费订单：走原有流程（`status=pending` → 微信支付 → `confirmed`）
- 免费订单不支持取消、退款、支付（后端已拦截）

---

## 二、数据库变更（生产环境 SQL）

本次共涉及 2 处变更：

### 1. `bookings` 表新增字段 `isFree`

```sql
ALTER TABLE bookings ADD COLUMN isFree boolean NOT NULL DEFAULT 0;
```

> 字段说明：
> - `isFree`：是否为免费订单。`1` = 免费，`0` = 收费。已有订单默认 `0`，不受影响。

### 2. `system_configs` 表更新 `paymentConfigJson` 配置

```sql
UPDATE system_configs
SET paymentConfigJson = '{"paymentAmount":0,"freeQuotaEnabled":false,"freeQuotaLimit":100}',
    updatedAt = strftime('%s','now') * 1000
WHERE configId = 'system_config';
```

> 字段说明（`paymentConfigJson` 内的三个字段）：
> | 字段 | 类型 | 默认值 | 说明 |
> |---|---|---|---|
> | `paymentAmount` | number | 0 | 每人收费金额（单位：分） |
> | `freeQuotaEnabled` | boolean | `false` | 是否开启「每日前N名免费」活动 |
> | `freeQuotaLimit` | number | 100 | 每日免费名额上限（去重用户数） |

> **注意**：
> - `paymentConfigJson` 是 JSON 字符串，若库里已有其他值（比如 `{"paymentAmount":500}`），请在此基础上合并新字段，不要覆盖掉 `paymentAmount` 的原值
> - `freeQuotaEnabled` 默认 `false`（功能关闭），**上线后需在管理后台手动开启**

### 验证 SQL

```sql
-- 检查 bookings 表新增字段
PRAGMA table_info(bookings);  -- 应包含 isFree 列

-- 检查系统配置
SELECT configId, paymentConfigJson FROM system_configs;
```

---

## 三、开启活动（管理员操作）

通过 `PUT /system-config` 接口下发配置：

```json
{
  "paymentConfig": {
    "paymentAmount": 100,
    "freeQuotaEnabled": true,
    "freeQuotaLimit": 100
  }
}
```

- `freeQuotaEnabled: true` → 开启每日免费活动
- `freeQuotaLimit: 100` → 每日 100 个免费名额（可随时调整）
- 关闭活动：把 `freeQuotaEnabled` 改为 `false` 即可，已产生的免费订单不受影响

---

## 四、接口变更

### 新增接口：查询免费名额状态（前端预约页调用）

```
GET /bookings/free-quota/status?bookingDate=2026-08-10
```

需携带用户 `Authorization: Bearer <token>`。`bookingDate` 可选，不传默认当天。

响应示例：

```json
{
  "success": true,
  "data": {
    "bookingDate": "2026-08-10",
    "freeQuotaEnabled": true,
    "freeQuotaLimit": 100,
    "freeQuotaUsed": 45,
    "freeQuotaRemaining": 55,
    "userCanGetFree": true,
    "userHasFreeBooking": false
  }
}
```

| 字段 | 说明 |
|---|---|
| `bookingIsToday` | 所查日期是否为今天（**仅今天可免费**，非今天则 `userCanGetFree` 恒为 `false`） |
| `userCanGetFree` | **前端判断依据**：当前用户这次预约是否免费（当天 且 名额未满 且 该用户当日无免费订单） |
| `freeQuotaRemaining` | 当日剩余免费名额 |
| `userHasFreeBooking` | 当前用户当日是否已有免费订单 |

前端逻辑：`userCanGetFree=true` → 展示「免费预约」标签，预约成功后直接提示成功（不调支付）；否则走原支付流程。若用户选择的预约日期不是今天，`userCanGetFree` 恒为 `false`，不应展示免费标签。

### 修改的接口行为

| 接口 | 变化 |
|---|---|
| `POST /bookings` | 免费名额内 → 直接返回 `status=confirmed` 的免费订单；超出名额 → 原流程 `status=pending` |
| `POST /bookings/:id/pay` | 免费订单被拒绝：`"该订单为免费预约，无需支付"` |
| `POST /bookings/:id/refund` | 免费订单被拒绝：`"免费预约无需退款"` |
| `DELETE /bookings/:id` | 免费订单被拒绝：`"免费预约不支持取消"` |

---

## 五、业务流程对比

### 免费用户（当天 + 名额内）

```
前端 GET /bookings/free-quota/status?bookingDate=今天 → userCanGetFree: true
  ↓
POST /bookings (bookingDate=今天) → 服务端事务内判断：当天 + 名额未满 + 无免费订单
  → isFree=true, status=confirmed, paymentStatus=paid
  ↓
前端直接展示「预约成功」          （不调用支付、不弹微信支付框）
```

### 收费用户（预约非当天日期 / 名额满 / 已有免费订单）

```
POST /bookings → status=pending, paymentStatus=unpaid, amount=人数×单价
  ↓
POST /bookings/:id/pay → 微信支付 → 前端 wx.requestPayment()
  ↓
微信回调 → status=confirmed
```

---

## 六、边界情况

| 场景 | 行为 |
|---|---|
| 预约未来日期（非当天） | **一律收费**，即使当天名额未满（免费只针对当天预约） |
| 预约当天日期 | 名额未满 且 该用户当天无免费订单 → 免费 |
| 免费名额从 100 调到 200 | 当天新用户可继续获得免费，直到满 200 |
| 免费名额从 100 调到 50 | 已产生的免费订单不受影响；新用户不再免费 |
| 关闭活动 `freeQuotaEnabled=false` | 所有新预约收费；已有免费订单保持免费 |
| 免费订单超时 | 不会超时（无支付超时时间，`paymentStatus=paid`，定时任务不会处理它） |
| 免费订单到游览日期后 | 正常流转为 `completed` |
| 并发抢最后一个免费名额 | 服务端事务 + SQLite 串行写保证不超卖 |
| 新的一天 | 名额重新计算，前一天的免费名额不影响今天 |

---

## 七、代码改动清单

| 文件 | 改动 |
|---|---|
| `src/entities/booking.entity.ts` | 新增 `isFree` 字段 |
| `src/entities/system-config.entity.ts` | `PaymentConfig` 接口新增 `freeQuotaEnabled` / `freeQuotaLimit` |
| `src/modules/system-config/dto/update-system-config.dto.ts` | `PaymentConfigDto` 新增两字段校验 |
| `src/repositories/system-config.repository.ts` | 默认配置 JSON 增加新字段 |
| `src/modules/booking/booking.service.ts` | `createBooking` 事务内判断免费名额；`initiatePayment`/`initiateRefund`/`deleteBooking` 拦截免费订单；新增 `getFreeQuotaStatus` |
| `src/modules/booking/booking.controller.ts` | 新增 `GET /bookings/free-quota/status` |

---

# 月卡会员功能说明

## 八、功能概述

管理后台人工录入月卡会员信息（绑定微信用户的 `wechatOpenId`），会员在小程序下单时自动享受免费，**不限次数**，**不占每日免费名额**，但受**每日最大预约数**控制。

### 规则明细

- 会员通过手机号录入，系统自动从 `user_profiles` 表反查 `wechatOpenId` 进行绑定
- 会员免费**不限次数**：一天可以下多单，每单都免费
- 会员免费**不占每日免费名额**：与「每日前 N 名免费」是两条独立的免费通道
- 会员免费受**每日最大预约数**控制：当天总预约人数达上限后，会员也无法预约
- 会员有有效期（`startDate` ~ `endDate`），过期自动失效
- 会员状态：`active`（生效中）/ `expired`（已过期）/ `disabled`（已停用）
- 会员可被管理员手动停用或删除
- **会员免费需身份校验**：下单时 `passengers` 中至少有一人的身份证号与会员记录一致，防止会员给任意人免费下单

### 免费判定优先级

```
1. 月卡会员 + 乘客身份匹配 → isFree=true, freeReason='member'    （不占每日免费名额，不限次数）
2. 每日前N名 → isFree=true, freeReason='dailyQuota'              （受每日免费名额限制）
3. 收费 → isFree=false, freeReason=null                          （走支付流程，含会员身份不匹配的情况）
```

---

## 九、数据库变更（生产环境 SQL）

### 1. 新建 `members` 表

```sql
CREATE TABLE members (
    id integer PRIMARY KEY AUTOINCREMENT,
    memberId varchar NOT NULL UNIQUE,
    wechatOpenId varchar NOT NULL,
    name varchar NOT NULL,
    phone varchar NOT NULL,
    idCard varchar NOT NULL,
    status varchar NOT NULL DEFAULT 'active',
    startDate integer NOT NULL,
    endDate integer NOT NULL,
    remarks varchar NOT NULL DEFAULT '',
    createdAt integer NOT NULL,
    updatedAt integer NOT NULL
);
CREATE INDEX IDX_members_wechatOpenId ON members (wechatOpenId);
CREATE INDEX IDX_members_phone ON members (phone);
```

### 2. `bookings` 表新增字段 `freeReason`

```sql
ALTER TABLE bookings ADD COLUMN freeReason varchar;
```

> 字段说明：
> - `freeReason`：免费来源。`'member'` = 月卡会员免费，`'dailyQuota'` = 每日免费名额，`NULL` = 收费订单

### 验证 SQL

```sql
-- 检查 members 表
PRAGMA table_info(members);

-- 检查 bookings 表新增字段
PRAGMA table_info(bookings);  -- 应包含 freeReason 列

-- 查询所有有效会员
SELECT memberId, name, phone, status, date(startDate/1000, 'unixepoch', '+8 hours') AS startDate, date(endDate/1000, 'unixepoch', '+8 hours') AS endDate
FROM members
WHERE status = 'active'
  AND startDate <= strftime('%s','now') * 1000
  AND endDate >= strftime('%s','now') * 1000;
```

---

## 十、管理后台 API

所有接口需要携带请求头 `x-admin-key: <ADMIN_API_KEY>`，即管理员登录后获取的 `apiKey`。

### 1. 创建月卡会员

```
POST /admin/members
```

**请求体：**

```json
{
  "phone": "13800138000",
  "name": "张三",
  "idCard": "110101199001011234",
  "startDate": "2026-08-01",
  "endDate": "2026-08-31",
  "remarks": "月卡会员-8月"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `phone` | string | ✅ | 会员手机号（必须已在小程序注册过，否则无法录入） |
| `name` | string | ✅ | 会员姓名 |
| `idCard` | string | ✅ | 会员身份证号 |
| `startDate` | string | ✅ | 有效期开始日期（YYYY-MM-DD） |
| `endDate` | string | ✅ | 有效期结束日期（YYYY-MM-DD，当天 23:59:59 失效） |
| `remarks` | string | ❌ | 备注 |

**成功响应：**

```json
{
  "success": true,
  "message": "月卡会员创建成功",
  "data": {
    "id": 1,
    "memberId": "a1b2c3d4-...",
    "wechatOpenId": "oXXXXXXX",
    "name": "张三",
    "phone": "13800138000",
    "idCard": "110101199001011234",
    "status": "active",
    "startDate": "2026-08-01T00:00:00.000Z",
    "endDate": "2026-08-31T23:59:59.999Z",
    "remarks": "月卡会员-8月",
    "createdAt": "2026-08-04T10:00:00.000Z",
    "updatedAt": "2026-08-04T10:00:00.000Z"
  }
}
```

**错误场景：**

| 场景 | 响应 |
|---|---|
| 手机号未在小程序注册 | `手机号 13800138000 尚未在小程序注册，无法录入会员。请该用户先使用小程序后再录入。` |
| 该用户已有生效中的会员 | `该用户已有生效中的月卡会员，请勿重复录入` |
| 开始日期晚于结束日期 | `开始日期不能晚于结束日期` |

### 2. 查询会员列表（分页）

```
GET /admin/members?keyword=张三&status=active&page=1&pageSize=10
```

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `keyword` | string | ❌ | 搜索关键字（匹配姓名/手机号/身份证号） |
| `status` | string | ❌ | 会员状态筛选（`active`/`expired`/`disabled`） |
| `page` | number | ❌ | 页码，默认 1 |
| `pageSize` | number | ❌ | 每页条数，默认 10 |

**响应：**

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "memberId": "a1b2c3d4-...",
      "wechatOpenId": "oXXXXXXX",
      "name": "张三",
      "phone": "13800138000",
      "idCard": "110101199001011234",
      "status": "active",
      "startDate": "2026-08-01T00:00:00.000Z",
      "endDate": "2026-08-31T23:59:59.999Z",
      "remarks": "月卡会员-8月",
      "createdAt": "2026-08-04T10:00:00.000Z",
      "updatedAt": "2026-08-04T10:00:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "pageSize": 10,
    "total": 1,
    "totalPages": 1
  }
}
```

### 3. 查询会员详情

```
GET /admin/members/:memberId
```

### 4. 更新会员信息

```
PUT /admin/members/:memberId
```

**请求体（所有字段可选）：**

```json
{
  "name": "张三（已更新）",
  "phone": "13900139000",
  "endDate": "2026-09-30",
  "status": "disabled",
  "remarks": "已停用-违规操作"
}
```

| 可更新字段 | 说明 |
|---|---|
| `name` | 会员姓名 |
| `phone` | 会员手机号 |
| `idCard` | 会员身份证号 |
| `startDate` | 有效期开始日期 |
| `endDate` | 有效期结束日期 |
| `status` | 会员状态（`active`/`expired`/`disabled`） |
| `remarks` | 备注 |

### 5. 删除会员

```
DELETE /admin/members/:memberId
```

> 删除后该用户不再享受会员免费。已产生的会员免费订单不受影响。

---

### 6. 查询当前用户会员状态（小程序端）

```
GET /member/status
```

需携带用户 `Authorization: Bearer <token>`。

**成功响应：**

```json
{
  "success": true,
  "data": {
    "isMember": true,
    "member": {
      "memberId": "a1b2c3d4-...",
      "name": "张三",
      "phone": "13800138000",
      "idCard": "110101199001011234",
      "startDate": "2026-08-01T00:00:00.000Z",
      "endDate": "2026-08-31T23:59:59.999Z",
      "status": "active",
      "daysRemaining": 25
    }
  }
}
```

**非会员响应：**

```json
{
  "success": true,
  "data": {
    "isMember": false,
    "member": null
  }
}
```

| 字段 | 说明 |
|---|---|
| `isMember` | 是否为有效会员 |
| `member.daysRemaining` | 会员剩余天数 |
| `member.startDate` | 有效期开始 |
| `member.endDate` | 有效期结束 |

> **前端用途**：展示会员标识、到期倒计时、判断是否显示会员专属 UI。

### 7. 校验乘客身份是否匹配会员（小程序端）

```
POST /member/verify
```

需携带用户 `Authorization: Bearer <token>`。

**请求体：**

```json
{
  "idCard": "110101199001011234"
}
```

**成功响应（匹配）：**

```json
{
  "success": true,
  "data": {
    "matched": true,
    "isMember": true,
    "name": "张三"
  }
}
```

**不匹配响应：**

```json
{
  "success": true,
  "data": {
    "matched": false,
    "isMember": true,
    "reason": "乘客身份证号与会员记录不一致"
  }
}
```

**非会员响应：**

```json
{
  "success": true,
  "data": {
    "matched": false,
    "isMember": false,
    "reason": "当前用户无有效会员"
  }
}
```

> **前端用途**：下单前校验乘客列表中是否有人是会员本人，决定是否展示「会员免费」标识。前端可遍历 passengers 逐个调用此接口，或在下单前对会员本人身份证做一次校验。

---

## 十一、管理后台页面对接指南

### 页面结构建议

在管理后台新增「月卡会员管理」菜单，包含以下页面：

#### 1. 会员列表页

```
┌──────────────────────────────────────────────────────────────────┐
│  月卡会员管理                                                     │
│                                                                    │
│  [搜索：姓名/手机号/身份证]  [状态：全部 ▼]  [查询]  [+ 新增会员] │
│                                                                    │
│  ┌────────┬──────────┬───────────┬─────────┬─────────┬─────────┐ │
│  │ 姓名   │ 手机号    │ 身份证号   │ 状态    │ 有效期   │ 操作    │ │
│  ├────────┼──────────┼───────────┼─────────┼─────────┼─────────┤ │
│  │ 张三   │ 138****0 │ 110***234 │ 生效中  │ 08/01~  │ 编辑 停用│ │
│  │        │          │           │         │ 08/31   │ 删除     │ │
│  │ 李四   │ 139****1 │ 120***567 │ 已停用  │ 07/01~  │ 编辑 启用│ │
│  │        │          │           │         │ 07/31   │ 删除     │ │
│  └────────┴──────────┴───────────┴─────────┴─────────┴─────────┘ │
│                                                                    │
│  共 2 条  第 1/1 页  < 1 >                                        │
└──────────────────────────────────────────────────────────────────┘
```

**接口调用：**
- 列表数据：`GET /admin/members?keyword=xxx&status=xxx&page=1&pageSize=10`
- 搜索/筛选：改变查询参数重新请求
- 停用/启用：`PUT /admin/members/:memberId` → `{ "status": "disabled" }` 或 `{ "status": "active" }`
- 删除：`DELETE /admin/members/:memberId`（需二次确认）

#### 2. 新增会员弹窗

```
┌──────────────────────────────────────┐
│  新增月卡会员                         │
│                                       │
│  手机号 *：[13800138000          ]   │
│  姓名 *  ：[张三                  ]   │
│  身份证 *：[110101199001011234   ]   │
│  开始日期：[2026-08-01          📅]   │
│  结束日期：[2026-08-31          📅]   │
│  备注    ：[月卡会员-8月         ]   │
│                                       │
│  ⚠️ 手机号必须为已在小程序注册的用户  │
│                                       │
│           [取消]    [确认创建]        │
└──────────────────────────────────────┘
```

**接口调用：**
- 提交：`POST /admin/members`，请求体如上文所示
- 错误处理：如果返回 `手机号 xxx 尚未在小程序注册`，提示管理员让该用户先打开小程序登录

#### 3. 编辑会员弹窗

与新增类似，但预填已有数据，调用 `PUT /admin/members/:memberId`

### 请求头配置

所有管理后台接口需携带：

```
x-admin-key: <管理员登录后获取的 apiKey>
```

管理员登录接口 `POST /admin/login` 返回的 `apiKey` 字段即为该值。

---

## 十二、业务流程对比（含会员）

### 月卡会员下单（乘客身份需匹配）

```
用户（会员）打开小程序 → JWT 登录（openid）
  ↓
POST /bookings (bookingDate=任意日期, passengers=[{idCard: "xxx"}])
  ↓
服务端事务内：
  1. 查 Member 表 → openid 是有效会员
  2. 校验 passengers 中至少一人 idCard 与会员记录一致 → 匹配
  3. → isFree=true, freeReason='member'
  4. 创建订单：status=confirmed, paymentStatus=paid, amount=0
  ↓
前端拿到 isFree=true → 直接展示「预约成功」（不调支付）
```

### 月卡会员下单（乘客身份不匹配 — 拒绝会员免费）

```
用户（会员）打开小程序 → JWT 登录（openid）
  ↓
POST /bookings (passengers 中所有人身份证都不是会员本人的)
  ↓
服务端事务内：
  1. 查 Member 表 → openid 是有效会员
  2. 校验 passengers 中无人 idCard 与会员记录一致 → 不匹配
  3. → 跳过会员免费，继续走每日免费名额 / 收费逻辑
  ↓
前端按正常逻辑处理（免费名额内 → 直接成功，否则 → 微信支付）
```

### 非会员 - 当天免费名额内

```
POST /bookings (bookingDate=今天)
  ↓
服务端：非会员 → 查每日免费名额 → 名额未满
  → isFree=true, freeReason='dailyQuota', status=confirmed
  ↓
前端直接展示「预约成功」
```

### 非会员 - 收费

```
POST /bookings (bookingDate=未来日期 或 名额已满)
  ↓
服务端：非会员 + 每日免费不命中
  → isFree=false, freeReason=null, status=pending, paymentStatus=unpaid
  ↓
前端 → POST /bookings/:id/pay → 唤起微信支付 → 回调 confirmed
```

---

## 十三、会员与各限额的关系

```
每日最大预约数（maxPeople = morningMaxPeople + afternoonMaxPeople）
├── 月卡会员订单 ──── 占名额 ✅（受总量控制）
├── 每日免费名额订单 ── 占名额 ✅
└── 收费订单 ──────── 占名额 ✅

每日免费名额（freeQuotaLimit = 100）
├── 每日免费名额订单 ── 占名额 ✅
└── 月卡会员订单 ──── 不占名额 ❌（独立通道）
```

---

## 十四、边界情况

| 场景 | 行为 |
|---|---|
| 会员预约任意日期 | 免费（不限制必须预约当天，与每日免费不同） |
| 会员一天下多单 | 每单都免费（不限次数） |
| 会员过期当天 | `endDate` 当天 23:59:59 前仍有效，次日失效 |
| 会员被停用（`status=disabled`） | 立即不再享受免费，走每日免费或收费逻辑 |
| 会员被删除 | 同上，已产生的免费订单不受影响 |
| 会员手机号未在小程序注册 | 录入时报错，无法创建 |
| 同一 openid 已有有效会员 | 拒绝重复录入 |
| 当天总预约人数已满 | 会员也无法预约（受 `maxPeople` 控制） |
| 会员帮他人代预约 | 拒绝会员免费：passengers 中无人身份证与会员记录一致 → 走每日免费或收费逻辑 |
| 并发下单 | 事务 + SQLite RESERVED 锁串行化，保证不超卖 |

---

## 十五、代码改动清单（月卡会员部分）

| 文件 | 改动 |
|---|---|
| `src/entities/member.entity.ts` | **新增**：月卡会员实体 |
| `src/repositories/member.repository.ts` | **新增**：会员数据访问层 |
| `src/modules/member/member.module.ts` | **新增**：会员模块 |
| `src/modules/member/member.service.ts` | **新增**：会员业务逻辑（含手机号反查 openid）；新增 `getMemberStatus`（用户端状态查询）、`verifyMemberIdentity`（身份证校验） |
| `src/modules/member/member.controller.ts` | **新增**：管理后台 CRUD 接口 |
| `src/modules/member/member-user.controller.ts` | **新增**：小程序用户端接口（`GET /member/status`、`POST /member/verify`） |
| `src/modules/member/dto/member.dto.ts` | **新增**：创建/更新/查询 DTO |
| `src/modules/member/dto/member-user.dto.ts` | **新增**：用户端校验 DTO |
| `src/entities/booking.entity.ts` | 新增 `freeReason` 字段 |
| `src/modules/booking/booking.service.ts` | `createBooking` 事务内优先判定会员免费 |
| `src/modules/booking/booking.module.ts` | 引入 `MemberModule` |
| `src/app.module.ts` | 注册 `MemberModule` 和 `Member` 实体 |
