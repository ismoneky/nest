# 每日前N名预约用户免费 —— 功能说明

## 一、功能概述

每天，预约日期为当日的订单中，**前 100 名（可配置）不同的预约用户** 享受免费，免费订单直接确认生效，**不走微信支付流程**。名额按预约日期每天独立计算，次日重置。

### 规则明细

- 按「去重用户」（`wechatOpenId`）计数，而非按订单数计数
- 同一用户当天（同一预约日期）最多享受一次免费，再次预约自动收费
- 免费订单：`isFree=true`、`amount=0`、`status=confirmed`、`paymentStatus=paid`，创建即生效
- 收费订单：走原有流程（`status=pending` → 微信支付 → `confirmed`）
- 免费订单不支持取消、退款、支付（后端已拦截）

---

## 二、数据库变更（生产环境 SQL）

本次共涉及 2 处变更：

### 1. `bookings` 表新增字段 `isFree`

```sql
ALTER TABLE bookings ADD COLUMN isFree boolean NOT NULL DEFAULT 0;
```

> 字段说明：
> - `isFree`：是否为免费订单。`1` = 免费，`0` = 收费。已有订单默认 `0`，不受影响。

### 2. `system_configs` 表更新 `paymentConfigJson` 配置

```sql
UPDATE system_configs
SET paymentConfigJson = '{"paymentAmount":0,"freeQuotaEnabled":false,"freeQuotaLimit":100}',
    updatedAt = strftime('%s','now') * 1000
WHERE configId = 'system_config';
```

> 字段说明（`paymentConfigJson` 内的三个字段）：
> | 字段 | 类型 | 默认值 | 说明 |
> |---|---|---|---|
> | `paymentAmount` | number | 0 | 每人收费金额（单位：分） |
> | `freeQuotaEnabled` | boolean | `false` | 是否开启「每日前N名免费」活动 |
> | `freeQuotaLimit` | number | 100 | 每日免费名额上限（去重用户数） |

> **注意**：
> - `paymentConfigJson` 是 JSON 字符串，若库里已有其他值（比如 `{"paymentAmount":500}`），请在此基础上合并新字段，不要覆盖掉 `paymentAmount` 的原值
> - `freeQuotaEnabled` 默认 `false`（功能关闭），**上线后需在管理后台手动开启**

### 验证 SQL

```sql
-- 检查 bookings 表新增字段
PRAGMA table_info(bookings);  -- 应包含 isFree 列

-- 检查系统配置
SELECT configId, paymentConfigJson FROM system_configs;
```

---

## 三、开启活动（管理员操作）

通过 `PUT /system-config` 接口下发配置：

```json
{
  "paymentConfig": {
    "paymentAmount": 100,
    "freeQuotaEnabled": true,
    "freeQuotaLimit": 100
  }
}
```

- `freeQuotaEnabled: true` → 开启每日免费活动
- `freeQuotaLimit: 100` → 每日 100 个免费名额（可随时调整）
- 关闭活动：把 `freeQuotaEnabled` 改为 `false` 即可，已产生的免费订单不受影响

---

## 四、接口变更

### 新增接口：查询免费名额状态（前端预约页调用）

```
GET /bookings/free-quota/status?bookingDate=2026-08-10
```

需携带用户 `Authorization: Bearer <token>`。`bookingDate` 可选，不传默认当天。

响应示例：

```json
{
  "success": true,
  "data": {
    "bookingDate": "2026-08-10",
    "freeQuotaEnabled": true,
    "freeQuotaLimit": 100,
    "freeQuotaUsed": 45,
    "freeQuotaRemaining": 55,
    "userCanGetFree": true,
    "userHasFreeBooking": false
  }
}
```

| 字段 | 说明 |
|---|---|
| `userCanGetFree` | **前端判断依据**：当前用户这次预约是否免费（名额未满 且 该用户当日无免费订单） |
| `freeQuotaRemaining` | 当日剩余免费名额 |
| `userHasFreeBooking` | 当前用户当日是否已有免费订单 |

前端逻辑：`userCanGetFree=true` → 展示「免费预约」标签，预约成功后直接提示成功（不调支付）；否则走原支付流程。

### 修改的接口行为

| 接口 | 变化 |
|---|---|
| `POST /bookings` | 免费名额内 → 直接返回 `status=confirmed` 的免费订单；超出名额 → 原流程 `status=pending` |
| `POST /bookings/:id/pay` | 免费订单被拒绝：`"该订单为免费预约，无需支付"` |
| `POST /bookings/:id/refund` | 免费订单被拒绝：`"免费预约无需退款"` |
| `DELETE /bookings/:id` | 免费订单被拒绝：`"免费预约不支持取消"` |

---

## 五、业务流程对比

### 免费用户（名额内）

```
前端 GET /bookings/free-quota/status → userCanGetFree: true
  ↓
POST /bookings → 服务端事务内判断名额 → isFree=true, status=confirmed, paymentStatus=paid
  ↓
前端直接展示「预约成功」          （不调用支付、不弹微信支付框）
```

### 收费用户（名额满 或 已有免费订单）

```
POST /bookings → status=pending, paymentStatus=unpaid, amount=人数×单价
  ↓
POST /bookings/:id/pay → 微信支付 → 前端 wx.requestPayment()
  ↓
微信回调 → status=confirmed
```

---

## 六、边界情况

| 场景 | 行为 |
|---|---|
| 免费名额从 100 调到 200 | 当日新用户可继续获得免费，直到满 200 |
| 免费名额从 100 调到 50 | 已产生的免费订单不受影响；新用户不再免费 |
| 关闭活动 `freeQuotaEnabled=false` | 所有新预约收费；已有免费订单保持免费 |
| 免费订单超时 | 不会超时（无支付超时时间，`paymentStatus=paid`，定时任务不会处理它） |
| 免费订单到游览日期后 | 正常流转为 `completed` |
| 并发抢最后一个免费名额 | 服务端事务 + SQLite 串行写保证不超卖 |
| 新的一天 | 名额重新计算（按预约日期），前一天的免费名额不影响今天 |

---

## 七、代码改动清单

| 文件 | 改动 |
|---|---|
| `src/entities/booking.entity.ts` | 新增 `isFree` 字段 |
| `src/entities/system-config.entity.ts` | `PaymentConfig` 接口新增 `freeQuotaEnabled` / `freeQuotaLimit` |
| `src/modules/system-config/dto/update-system-config.dto.ts` | `PaymentConfigDto` 新增两字段校验 |
| `src/repositories/system-config.repository.ts` | 默认配置 JSON 增加新字段 |
| `src/modules/booking/booking.service.ts` | `createBooking` 事务内判断免费名额；`initiatePayment`/`initiateRefund`/`deleteBooking` 拦截免费订单；新增 `getFreeQuotaStatus` |
| `src/modules/booking/booking.controller.ts` | 新增 `GET /bookings/free-quota/status` |
